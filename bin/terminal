#!/bin/bash

PERLLIB=$(cd $(dirname $0)/../dotfiles/urxvt && pwd)
EN_FONT=$(test -n "$xft" && echo "$xft" || echo "Courier Prime Code")

/usr/bin/urxvt \
    --perl-lib $PERLLIB -pe newterm \
    -lsp 4 -letsp -3 \
    -fn "xft:$EN_FONT:size=11,xft:Everson Mono Unicode,xft:CloudYuanZhunGBK:size=9" \
    $@ \
    -e bash
