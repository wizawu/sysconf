#!/bin/bash

sudo chattr -i /etc/resolv.conf

if [ -n "$1" ]; then
    sudo rm /etc/resolv.conf
    for s in $@; do
        echo nameserver $s | sudo tee -a /etc/resolv.conf
    done
else
    cat /etc/resolv.conf
fi
