#!/bin/bash

# reset
setxkbmap -layout us

# map Alt_R to Mode_switch
xmodmap -e "keycode 108 = Mode_switch"

# map Shift+Escape to ~
# map Mode_switch+Escape to `
xmodmap -e "keycode 9 = Escape asciitilde grave"
