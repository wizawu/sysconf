#!/bin/bash

if [[ "$1" = ~* ]]; then
    PUSH_HEAD=HEAD$1
    shift
else
    PUSH_HEAD=HEAD
fi

if [[ "$1" = "" ]]; then
    set -x
    git push origin $PUSH_HEAD:refs/for/master
else
    set -x
    git push origin $PUSH_HEAD:refs/for/$@
fi
