file-roller                # archive graphical manager
dstat                      # combination of stat tools
iftop
nmap
xsel
httrack
axel
libpdf.so                  # copy to /usr/lib/chromium
fxchrome                   # chrome theme for firefox
vimerl                     # vim plugin for Erlang
clang_complete             # vim plugin for c
siege
http_load
httperf
vifm
curl icanhazip.com
scp
rsync
htop                       # powerful process viewer
calcurse                   # to-do manager
tpp                        # console ppt
ncat
strace                     # trace system calls
fcitx-cloudpinyin
hdparm --fibmap [...]      # block address of a file
lshw -C cpu|network|...    # 
xev                        # print key code
xmodmap                    # modify keymaps
lzz                        # Lazy C++
vimacs
pkgfile
wps
pmap                       # report memory map of a process
goagent                    # find it on github
set -x                     # trace commands executed
losetup                    # set up loop devices
iotop
perf stat|top|...          # bench, lock, kmem
lscpu
lsusb
fio
libhugetlbfs-utils         # hugeadm, hugectl, pagesize...
